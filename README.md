# Julia-Program
A Julia program that reads basketball player stats from a file, calculates eFG% and TS%, flags invalid entries, and prints two reports—one sorted by player name and another by TS% in descending order.
